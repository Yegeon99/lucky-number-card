# 럭키 넘버 카드 · 클로드 코드 제작 지침서 (데모 v0.1)

이 문서는 클로드 코드가 그대로 따라 만들 수 있게 쓴 지침이다. 함께 제공되는 PRD(럭키넘버카드_PRD.md)를 먼저 읽고, 충돌이 있으면 PRD의 "절대 원칙"이 우선한다.

---

## 0. 작업 규칙 (모든 단계에 적용)

1. **단계별 관문 방식.** 아래 1단계부터 8단계까지 순서대로 진행한다. 각 단계 끝의 "통과 조건"을 실제로 확인(빌드·실행·화면 확인)한 뒤에만 다음 단계로 간다. 통과 못 하면 스스로 원인을 찾아 고치고 다시 확인한다.
2. **질문 최소화.** 기술 판단은 스스로 한다. 사용자에게 묻는 것은 기능 방향의 갈림길일 때만이며, 그때도 선택지 2~3개와 추천안을 함께 제시한다.
3. **보고 형식.** 각 단계가 끝나면 비개발자가 이해할 수 있는 한국어로 3줄 이내 보고: 무엇을 만들었는지, 어떤 문제가 있었고 어떻게 고쳤는지(있다면 한 문장), 확인 방법(주소 또는 화면 위치). 기술 용어 나열 금지.
4. **문장 규칙.** 화면 문구와 보고 문장에 줄표(—, –, ㅡ)와 하이픈으로 단어를 잇는 표현을 쓰지 않는다. 화면 문구에서 "추첨, 뽑기, 당첨, 미당첨, 꽝, 확률, 가품"은 금지 단어다(럭키 넘버 안내 페이지의 법적 고지에서 "수량, 비율"만 허용).
5. **품질 기준.** 임시 디자인, 기본 템플릿 느낌, "일단 되게만" 금지. 여백·정렬·서체·색·동작 하나하나가 프리미엄 상품 화면처럼 보여야 한다. 완성 후 폰 화면(가로 390px 기준)으로 반드시 확인한다.
6. **사진은 서버로 보내지 않는다.** 카메라·갤러리 이미지와 합성 결과는 브라우저 안에서만 처리하고 어떤 요청에도 담지 않는다. 이 규칙을 위반하는 코드는 작성하지 않는다.
7. **교체 가능 구조.** 정품 검증은 데모용 모의 함수로 만들되, 정식 검증(보안 칩 서명 확인)으로 바꿀 때 함수 하나만 교체하면 되게 입력·출력 형태를 고정한다.

---

## 1단계. 프로젝트 골격

**기술 선택 (변경하지 말 것)**
- Next.js(앱 라우터) + React + Tailwind CSS v4 + Framer Motion(연출) + Zustand(화면 상태).
- 서버 저장은 데모 단계에서 단일 JSON 파일 기반 저장소(카드 목록·등록 상태·확인 횟수)로 시작한다. 파일 저장 모듈을 한 곳으로 모아 나중에 데이터베이스로 바꿀 때 그 모듈만 교체하면 되게 한다.
- 도감은 브라우저 저장소(localStorage)에 저장한다. 계정 없음.
- 서체: Pretendard(웹폰트). 배포: Vercel 기준.

**폴더 구성(권장)**
- app/ : 화면(카드 홈, 네컷 스튜디오, 도감, 럭키 안내, 관리)
- app/api/ : 카드 조회·등록·확인 처리
- lib/verify/ : 정품 검증(모의 함수 + 정식 함수 자리)
- lib/store/ : 서버 저장 모듈(JSON 파일)
- lib/frames/ : 프레임 정의(카드별 프레임, 금색 테두리, 단체 프레임)
- data/cards.json : 샘플 카드 목록
- public/sample/ : 이미지 자리(공식 이미지가 들어갈 빈 자리 + 안내 문구 이미지)

**통과 조건**: 개발 서버가 뜨고, 빈 카드 홈 화면이 폰 크기에서 오류 없이 표시된다.

---

## 2단계. 데이터와 검증 모의

**카드 목록(data/cards.json) 필수 항목**
- 카드 식별자, 일련번호(예: 0412), 발행 수량(예: 3000), 아티스트, 앨범, 멤버, 발행일, 제작사("포레스트팩토리"), 카드 컷 이미지 경로, 전용 프레임 식별자, 세트 식별자, 의미 번호 여부와 설명(선택), 럭키 여부(서버만 아는 값. 화면에 내려 보낼 때는 첫 등록 판정 결과와 함께 최소 정보만).
- 샘플 카드 최소 8장: 한 세트 7장(멤버별) + 럭키 카드 1장 포함. 의미 번호 예시 1장(0001) 포함.

**등록 상태 저장(서버 JSON)**: 카드별 첫 등록 기기 토큰, 등록 시각, 누적 확인 횟수, 마지막 확인 시각.

**기기 토큰**: 첫 방문 시 브라우저에 임의 토큰을 만들어 저장. 이 토큰이 "첫 등록자" 판정 기준이다. 개인정보가 아니다.

**검증 함수 규칙(lib/verify)**
- 입력: 주소에서 받은 문자열(카드 식별자 + 토큰 부분), 기기 토큰.
- 출력: 상태 세 가지 중 하나(정품 확인 / 확인 불가 / 다른 기기 등록) + 카드 공개 정보 + 이 기기가 첫 등록자인지 + 럭키 표시 여부(첫 등록자일 때만 true) + 의미 번호 정보.
- 데모 모의: 카드 목록에 있고 토큰 형식이 맞으면 정품. 목록에 없거나 형식이 틀리면 확인 불가. 다른 기기 토큰으로 이미 등록되어 있으면 다른 기기 등록.
- 정식 함수 자리: 같은 입력·출력으로 비워 두고 주석에 "보안 칩 서명 확인, 읽기 횟수 역행 차단으로 교체" 명시.

**데모용 주소 형태**: /c/[카드식별자]?t=[토큰]. NFC 카드에는 이 주소를 기록한다.

**통과 조건**: 주소 3종(정품 / 없는 카드 / 다른 기기 등록 시나리오)으로 접근했을 때 API가 세 상태를 각각 정확히 돌려준다.

---

## 3단계. 카드 홈 화면

**구성(위에서 아래)**
1. 상단 고정 띠(스크롤해도 고정): 상태 아이콘 + 문구 + "No. 0412 / 3000". 상태별 색 3종만 사용(초록 계열 / 붉은 계열 / 노란 계열). 누르면 상세 카드가 아래로 펼쳐진다(아티스트·앨범, 순번, 발행일, 제작 포레스트팩토리, 도감 등록 여부, 누적 확인 횟수).
2. 카드 컷: 화면 중앙, 실물 포토카드 비율(55×85 기준), 은은한 그림자와 광택. 이미지가 없으면 "샘플 이미지 자리" 표시.
3. 카드 번호: 큰 글씨. 오른쪽에 배지가 붙을 빈 자리 확보.
4. 네컷 스튜디오 시작 버튼: 화면에서 가장 큰 버튼.
5. 내 도감 바로가기.
6. 하단 소형 링크: 럭키 넘버 안내, 문의.

**상태별 동작**
- 확인 불가: 네컷 버튼 비활성 + "재시도", "문의" 버튼. 문구 "확인할 수 없는 카드입니다". "가품" 단어 금지.
- 다른 기기 등록: 네컷 버튼 활성. 도감 등록·럭키 확인 잠김. 문구 "다른 기기에서 등록된 카드입니다".
- 정품 확인 + 첫 등록: 자동으로 도감에 등록하고 등록 안내를 작게 표시.

**통과 조건**: 세 상태 화면이 폰 크기에서 각각 정확히 보이고, 띠가 스크롤 중에도 고정된다.

---

## 4단계. 럭키 넘버·의미 번호 연출

- 조건: 정품 확인 + 이 기기가 첫 등록자 + 럭키 카드. 이 세 조건이 모두 맞을 때만 연출한다. 아닐 때는 어떤 문구도 표시하지 않는다(이 점을 코드 주석으로 명시).
- 연출: 카드 홈이 완전히 그려진 뒤 1초 대기 → 번호 옆 빈 자리로 금색 LUCKY NUMBER 배지가 오른쪽에서 미끄러져 들어오며 번호 글자에 금색이 입혀진다. 전체 1.5초 이내. 소리 없음. 회전·슬롯·룰렛 금지.
- 배지 누름 → 혜택 안내 카드(바닥에서 올라오는 시트): 혜택 내용, 수령 방법, 마감일, "응모하기" 버튼(데모는 "기획사 응모 페이지로 연결됩니다" 안내 문구로 대체).
- 첫 등록자가 아닌 태그: 배지는 정적으로 표시되되 눌러도 혜택 안내가 열리지 않고 "이 카드는 럭키 넘버 카드입니다" 한 줄만.
- 의미 번호: 은색 배지 + 한 줄 설명(예: "데뷔일 번호"). 연출은 나타남만, 지연 없음.
- 럭키 카드는 네컷 프레임 목록에 금색 테두리 버전 1종을 추가 해금한다.

**통과 조건**: 럭키 카드 첫 방문에서 연출이 기본 화면을 가리지 않고 나중에 붙는다. 같은 카드를 다른 기기 토큰으로 열면 혜택 안내가 열리지 않는다. 럭키가 아닌 카드에는 아무 문구도 없다.

---

## 5단계. 네컷 스튜디오

**촬영**
- 브라우저 카메라(전면 기본, 전후 전환). 권한 요청 전에 한 줄 안내 표시. 거부 시 갤러리 불러오기로 자연스럽게 안내.
- 카운트다운 3초, 4컷 자동 연속 촬영. 각 컷 재촬영 가능. 갤러리에서 4장 선택도 지원.
- 촬영·불러온 이미지는 메모리에만 둔다. 서버 전송 코드 금지.

**합성**
- 캔버스에서 합성. 기본 배치 세로 4컷(포토부스 표준 비율, 2:6 기준), 옵션 2×2.
- 프레임 정의(lib/frames): 카드별 기본 프레임, 금색 테두리(럭키), 단체 프레임(세트 완성). 프레임은 레이어 그림으로 정의하고 사진 구멍 위치를 좌표로 지정.
- 프레임 요소: 아티스트 컷 테두리 이미지(자리), 앨범 로고(자리), 날짜 자동, 카드 일련번호 소형 표기.
- 결과 해상도: 긴 변 2000px 상한.

**결과 화면**
- 미리보기(실제 포토부스 출력물처럼 흰 여백, 하단 날짜·로고).
- 버튼: 저장, 배경화면 비율 자르기(폰 화면 비율), 공유(웹 공유 기능, 미지원 브라우저는 저장 안내).
- 아이폰 사파리에서는 "이미지를 길게 눌러 저장" 안내를 함께 표시.

**통과 조건**: 아이폰 사파리와 안드로이드 크롬에서 촬영 → 합성 → 저장까지 끊김 없이 된다. 네트워크 탭에서 이미지가 서버로 나가는 요청이 없음을 확인한다.

---

## 6단계. 내 도감

- 저장: 브라우저 저장소. 카드 식별자, 등록 시각, 배지 종류.
- 화면: 세트별 카드 격자. 보유 카드는 썸네일·번호·배지, 미보유는 흐림 처리(어떤 카드인지는 보이되 흐리게).
- 세트 완성률(예: 7장 중 3장) 표시. 완성 시 단체 프레임 해금 안내와 네컷 스튜디오 바로가기.
- 삭제(데모 초기화 겸용) 버튼: 확인 후 도감 비우기.

**통과 조건**: 카드 3장을 차례로 열면 도감이 채워지고 완성률이 바뀐다. 7장을 모두 열면 단체 프레임이 프레임 목록에 나타난다.

---

## 7단계. 럭키 넘버 안내(법적 고지)와 관리 화면

**럭키 넘버 안내(/notice)**
- 항목: 총 발행 수, 럭키 넘버 수량과 등급, 사전 공개 해시값(럭키 번호 목록의 SHA-256, 데모는 샘플 목록으로 실제 계산), 원본 공개 예정일, 응모 마감, 문의처.
- 문체: 사실 나열. 여기서만 "수량, 비율" 표기 허용. 그 외 금지 단어는 여기서도 금지.

**관리 화면(/admin)**
- 간단한 비밀번호 1개(환경 변수).
- 카드 목록 표: 번호, 상태(미등록/등록/럭키), 누적 확인 횟수, 마지막 확인 시각.
- 데모 초기화 버튼: 서버 등록 상태 전체 해제.

**통과 조건**: 안내 페이지의 해시값이 data 안 럭키 목록으로 재계산했을 때 일치한다. 관리 화면에서 초기화 후 카드를 다시 열면 첫 등록 연출이 다시 나온다.

---

## 8단계. 마감 점검과 데모 준비

- 폰 실기기 점검표: 첫 화면 3초 이내, 띠 고정, 세 상태 색·문구, 럭키 연출 타이밍, 카메라 권한 거부 경로, 저장 안내, 도감 반영, 해시 일치.
- 금지 단어 전수 검색: "추첨, 뽑기, 당첨, 미당첨, 꽝, 확률, 가품"이 화면 문구에 없는지 확인(안내 페이지의 "수량, 비율" 제외).
- 줄표·하이픈 연결 표현 전수 검색.
- 이미지 자리마다 "샘플 이미지, 실제 상품과 무관" 표기 확인.
- 데모 카드 주소 3개(정품 일반, 럭키, 의미 번호)를 QR 이미지로도 만들어 outputs에 저장(NFC 기록 전 임시 시연용).
- 최종 보고: 배포 주소, 데모 카드 주소 3개, 관리 비밀번호 위치, 이미지 교체 방법(폴더와 파일명), 정식 검증으로 바꿀 때 교체할 파일 1개의 위치.

---

## 부록 A. 화면 문구 (그대로 사용)

- 정품 띠: "정품 · No. {번호} / {발행수량}"
- 확인 불가: "확인할 수 없는 카드입니다" / 버튼 "재시도", "문의"
- 다른 기기 등록: "다른 기기에서 등록된 카드입니다"
- 도감 등록 안내: "내 도감에 담았어요"
- 럭키 배지: "LUCKY NUMBER"
- 럭키 안내 시트 제목: "이 번호에는 플러스 혜택이 있어요"
- 럭키 카드 재태그: "이 카드는 럭키 넘버 카드입니다"
- 의미 번호 예시: "데뷔일 번호", "첫 번째 카드", "{멤버} 생일 번호"
- 네컷 시작: "네컷 찍기"
- 카메라 안내: "사진은 내 폰에만 저장돼요. 카메라 사용을 허용해 주세요"
- 저장 안내(아이폰): "이미지를 길게 눌러 저장하세요"
- 세트 완성: "세트를 모두 모았어요. 단체 프레임이 열렸어요"
- 이미지 자리: "샘플 이미지, 실제 상품과 무관"

## 부록 B. 색과 서체

- 서체: Pretendard. 숫자는 표 정렬 숫자 사용.
- 상태 색: 정품 초록 계열 1색, 확인 불가 붉은 계열 1색, 다른 기기 노란 계열 1색. 그 외 화면은 흰 배경 + 짙은 회색 글자 + 카드 컷 색감이 주인공.
- 배지: 럭키는 금색 그라데이션, 의미 번호는 은색. 금속 질감은 은은하게.

## 부록 C. 정식 단계로 갈 때 바뀌는 것 (참고용, 지금 만들지 않음)

- lib/verify의 모의 함수를 보안 칩 서명 확인 함수로 교체.
- JSON 저장 모듈을 데이터베이스 모듈로 교체.
- 응모 버튼을 기획사 응모 폼 주소로 연결.
- 소유 이전, 이메일 백업 추가.
